# E08 Unified UI Real-Browser Qualification

Item | State | Notes
--- | --- | ---
E08.1 API v3 contract coverage | `[+]` | Tests reject v1/v2 routes and malformed requests, prove access-code/auth policy, strict multipart bounds, independent revisions, complete ETags, response-replay rejection, non-blocking preflight polling, one-capture mutation locking, shutdown, and execution-route absence.
E08.2 Acquisition and review coverage | `[+]` | Engine tests cover upload validation/staging/removal, secrets, ambiguity, cleanup, restart, collisions, digest ownership, append-only v3 review, deterministic v2 migration, external review drift, and multiple non-overwriting profile pairs.
E08.3 Worker and process coverage | `[+]` | Browsertools and OpenUdon tests cover shared runner behavior, required boundary inputs, stable executable copies, driver propagation, minimal secret-free environment, close negotiation, malformed protocol, cancellation, idle timeout, stdout draining, and descendant termination.
E08.4 Package recovery and handoff coverage | `[+]` | Tests require explicit build confirmation, exercise check-specific failure, resume and repeated authoring approval, verify current output bytes, enforce the artifact allowlist, invalidate handoff on later artifact drift, and confirm no approval/run route exists.
E08.5 Terminal compatibility coverage | `[+]` | Bundled live mode remains the default, the expert external override remains available, and combined completion-observation/checkpoint delivery is covered without launching Chromium.
E08.6 Credential-leak qualification | `[+]` | Typed HTTP/worker structures, minimal-environment/process tests, profile/review/package inspection tests, and browser DOM assertions cover the complete value-flow surface without exposing credential, challenge, session, private-path, worker-output, argv, or environment values. Real Chromium adds the UI boundary while API/engine tests retain deterministic capture-through-handoff coverage.
E08.7 Real sandboxed Chromium lifecycle | `[+]` | All 13 production-loopback UI journeys pass with `chromium_sandbox_enabled=true sandbox_required=true`, including access-code bootstrap, final and incomplete approval, acquisition/capture reconciliation, package-ready transition, drift/retry/lockout, polling, and responsive behavior. The disable override is rejected and was not used; deterministic API/engine tests cover the remaining build and frozen-handoff transitions.
E08.8 Release and standalone gates | `[+]` | Published Browsertools A06 is pinned, and E09 completes the early and cold-cache standalone builds, full test/vet/race/release checks, pinned deadcode, strict docs/boundaries, six CGO-disabled cross-build targets, clean-root browser matrices, and sandbox-required UI gate.
E08.9 Authenticated-authoring adversarial matrix | `[+]` | Focused tests reject inserted/removed/reordered trace facts, changed authentication locators, fabricated outputs, final-origin mismatch, stale/noncanonical approvals, unsafe configured/observed/frame paths, context/diagnostic overflow, and missing process-private attestation; the loopback corpus adds path-injection and fabricated-trace rejection cases.
E08.10 Hardened release rerun | `[~]` | The hardened Browsertools revision is published; fresh proxy-only resolution, standalone OpenUdon build/test/vet, full Browsertools tests/vet, full workspace OpenUdon tests/vet, iCoT race, project/boundary/doc-memory checks, browser integration, and the eight-case headless journey matrix pass at clean revisions. This host's disabled unprivileged-user-namespace policy still blocks the sandbox-required UI and headed loopback launches, so neither is recorded as a pass.

`not_run` is not a pass. The separately named sandbox-disabled target remains
diagnostic only and did not contribute to this qualification.
