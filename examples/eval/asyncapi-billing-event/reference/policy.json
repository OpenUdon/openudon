{
  "mode": "strict",
  "notes": [
    "Strict AsyncAPI fixture: package-local event source binds to explicit body/header request mappings and remains provider-free."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive AsyncAPI seed fixture must materialize and build successfully from its reference intent."
  }
}
