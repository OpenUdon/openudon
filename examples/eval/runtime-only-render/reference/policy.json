{
  "mode": "strict",
  "notes": [
    "The runtime-only render fixture is stable; fnct runtime selection and input flow are the golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
