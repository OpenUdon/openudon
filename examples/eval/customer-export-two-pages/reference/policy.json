{
  "mode": "strict",
  "notes": [
    "The two-page export fixture is stable; the two page fetches and merge adapter are the golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
