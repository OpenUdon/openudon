openapi: 3.0.0
info:
  title: Support API
  version: 1.0.0
paths:
  /tickets/{ticketId}:
    get:
      operationId: getTicket
      summary: Fetch support ticket details
      parameters:
        - name: ticketId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Ticket details
          content:
            application/json:
              schema:
                type: object
                properties:
                  requesterEmail:
                    type: string
                  subject:
                    type: string
                  summary:
                    type: string
