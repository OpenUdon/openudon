{
  "mode": "strict",
  "notes": [
    "Timeout and workflow idempotency metadata are opt-in and must be preserved when requested by openudon-policy."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
