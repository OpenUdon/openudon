{
  "mode": "strict",
  "notes": [
    "Strict native SaaS authoring fixture; geocoding before weather lookup, appid credential binding, and response-to-request mapping are golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
