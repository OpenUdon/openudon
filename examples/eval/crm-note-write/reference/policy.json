{
  "mode": "strict",
  "notes": [
    "The CRM write fixture is stable; operation selection and write-policy evidence are the golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
