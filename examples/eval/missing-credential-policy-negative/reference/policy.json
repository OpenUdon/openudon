{
  "mode": "strict",
  "notes": [
    "Negative fixture: missing credential policy must become a local gap report, not an unaudited API call."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "expected-negative",
    "reason": "Negative or repair seed fixture should materialize a reviewable package without executing disallowed behavior."
  }
}
