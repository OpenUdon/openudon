{
  "mode": "strict",
  "notes": [
    "Idempotent side-effectful webhook fixture with timeout controls is golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
