{
  "mode": "strict",
  "notes": [
    "Strict OData fixture: package-local operations-service CSDL source binds to the Customers entity set and reviewed query-option mapping."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive OData seed fixture must materialize and build successfully from its reference intent."
  }
}
