openapi: 3.0.0
info:
  title: Jira Incident API
  version: 1.0.0
servers:
  - url: https://jira.example.test
paths:
  /rest/api/3/issue:
    post:
      operationId: createIssue
      summary: Create an incident issue.
      security:
        - jiraBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - summary
                - description
                - severity
              properties:
                summary:
                  type: string
                description:
                  type: string
                severity:
                  type: string
                service:
                  type: string
      responses:
        "201":
          description: incident issue created
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  self:
                    type: string
components:
  securitySchemes:
    jiraBearer:
      type: http
      scheme: bearer
