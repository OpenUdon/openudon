{
  "mode": "strict",
  "notes": [
    "The profile boundary fixture is stable; OpenUdon must use a trusted fnct manifest instead of inventing SQL, SSH, or x-udon profile runtime semantics."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
