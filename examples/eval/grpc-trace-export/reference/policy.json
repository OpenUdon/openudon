{
  "mode": "strict",
  "notes": [
    "Strict gRPC/protobuf fixture: package-local OpenTelemetry proto source binds to the TraceService Export method and request-body field mapping."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive gRPC/protobuf seed fixture must materialize and build successfully from its reference intent."
  }
}
