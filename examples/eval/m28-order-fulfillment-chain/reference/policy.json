{
  "mode": "strict",
  "notes": [
    "Strict multi-service SaaS pattern fixture; per-service OpenAPI selection, response extraction, request body construction, credential scopes, and write-policy evidence are golden."
  ],
  "max_blocking": 0,
  "max_warning": 0,
  "max_unresolved_review": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
