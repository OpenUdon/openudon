{
  "mode": "strict",
  "notes": [
    "OpenUdon-native IT Ops fixture inspired by the n8n workflow-backup template; operation selection and side-effect policy are golden behavior."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
