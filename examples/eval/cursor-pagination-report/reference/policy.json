{
  "mode": "strict",
  "notes": [
    "The cursor-pagination fixture is stable; cursor extraction, bearer credential binding, and two-page fetch behavior are golden."
  ],
  "max_blocking": 0,
  "seed_build": {
    "expected": "pass",
    "class": "strict-positive",
    "reason": "Strict positive seed fixture must materialize and build successfully from its reference intent."
  }
}
