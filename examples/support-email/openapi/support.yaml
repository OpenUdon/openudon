openapi: 3.0.0
info:
  title: Support API
  version: 1.0.0
  description: Sandbox support-ticket API used by the OpenUdon support email example.
servers:
  - url: https://support.example.test
paths:
  /tickets/{ticketId}:
    get:
      operationId: getTicket
      summary: Fetch support ticket details
      parameters:
        - name: ticketId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Ticket details
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  requesterEmail:
                    type: string
                    format: email
                  subject:
                    type: string
                  summary:
                    type: string
