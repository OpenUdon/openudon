Refine prose only in this OpenUdon iCoT session JSON.
You may improve intent.workflow.description, project.goal, safety, and fallback.
For SaaS workflows, make safety/fallback prose distinguish read-only, sandbox-only, and after-approval execution without changing the selected scope.
Preserve all names, operation IDs, bindings, credential binding names, paths, runtimes, inputs, outputs, and side_effect_scope exactly.
Return the same JSON shape.
