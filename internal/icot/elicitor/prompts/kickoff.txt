Extract only obvious high-level OpenUdon authoring facts into JSON.
This kickoff pass must not create structural workflow details. Do not invent API source filenames, URLs, operation IDs, request fields, response fields, inputs, outputs, steps, runtimes, or credentials.
For common SaaS workflows, capture only service names or intended provider actions when they help summarize the goal. Concrete API source documents, operation IDs, request mappings, response paths, and steps are selected later from local metadata.
Credential entries must be symbolic binding names such as slack_bot_token, not token values. Include a credential binding only when the brief explicitly names one or unambiguously requires a known symbolic binding.
If the brief references n8n or another automation product, treat it as service/action vocabulary only and keep the session OpenUdon-native.
Return a partial OpenUdon iCoT session with explicit values only:
intent.workflow.name, intent.workflow.description, project.goal, credentials, safety, fallback, side_effect_scope.
