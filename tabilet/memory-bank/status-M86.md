# M86 — UWS 1.11 real-browser qualification

**Goal.** Qualify the published M85 stack with sandboxed real browsers and value-free current-stack scenario evidence.

**Provenance.** Source: “UWS 1.11 real-browser qualification” (pasted owner task), findings F01–F08. Source priority was not supplied; each finding is locally P2. Review baseline and revalidation commit: clean OpenUdon `efa48ccb0e843f6942dfa9e41a1069a5735eb368`. Browsertools `9333a9f25dbb17551998a429e123e7a9ba976648`, Browserdriver `8c13b70d30a500e65e90a95a203493301b8b21a5`, and UWS `e9b6181be0abb7f683fdb624d4dba282a59991d1` were clean at their published pins. The desktop Udon checkout was clean but at `81fff2c91b533913e4b48523b97fb597551283f9`; the published `080b8282e2b8f7ca7a9994b6d9f0e3d2891d853f` was absent locally. No uncommitted source edits were used as evidence. M85's default matrix pass and E04/E05 historical scenario qualification remain complete, unchanged history.

**Findings.** F01: `internal/browserscenario/compatibility-lock.json` fixes older sources, so current scenarios need a separate lock and report contract. F02: the existing corpus has 23 loopback and eight journey cases but no real Browser 1.8/1.9 template or mixed v10 case. F03: M85 passed 16 required gates while all three browser opt-ins were unrequested. F04: the desktop Udon checkout does not match the published pin. F05: root-run `check-doc-memory` fails because the checker expects `tabilet/` as cwd, while the same check passes there. F06: the scenario Node readiness probe omitted `chromiumSandbox: true` even though Browsertools and Browserdriver runtime launches require it. F07: the hosted release job omits UWS checkout and stages unignored dependency clones beneath the source root. F08: the first 11-case journey run passed 10 and failed the Browser 1.8 maximum-integer case because the HCL datafile input was rejected before v10 replay; the reviewed profile-default path retains the exact integer.

**Scope.** Local synthetic targets only. Preserve the historical scenario lock, v1 reports and verifier, all completed statuses, and public canaries. Do not use real accounts, provider endpoints, public canaries, runtime adoption, or unsandboxed browser retries.

| Item | State | Notes |
|---|---|---|
| M86.1 | `[+]` | Added the separate exact current lock, v2 report dispatch, complete-suite passing verifier, strict current manifests, and explicit CLI stack selection. Historical lock, corpus and v1 verifier remain unchanged. Focused report checks pass. F01. |
| M86.2 | `[+]` | Added three local modern journeys with reviewed profile validation, multi-source synthesis, v10 replay selection, exact server postconditions and sandboxed Node readiness. The historical 23+8 manifests and guide path remain intact; focused synthesis tests pass. Full real-browser acceptance remains M86.6. F02/F06. |
| M86.3 | `[+]` | Make and hosted release scenarios select current; hosted setup checks out pinned UWS and ignores generated sibling clones. Corrected doc-memory commands to run from `tabilet/`, and updated evaluator/operator/memory guidance. Doc-memory and diff checks pass; strict MkDocs/actionlint tools were installed in temporary local paths for M86.7. F05/F07. |
| M86.4 | `[+]` | At clean OpenUdon `46396c228c335f3abc201d6f572357dca8ea25b4` and the exact sibling pins below, Chromium 151.0.7922.34 launched with `chromiumSandbox: true`, Firefox 153.0 and WebKit 26.5 launched. `browser-integration-eval --installed-engines --headed-auth` passed and independently verified 19/19 gates, zero failed/skipped; SHA-256 `69feb11ba4742b43e12961b98a1b79d64fe304ce917880c480e62a91d4d7b469`. F03/F04. |
| M86.5 | `[+]` | The first current journey report passed 10/11, with Browser 1.8 replay rejecting the maximum signed integer supplied through the HCL datafile. The case now binds that exact value from the reviewed Browser 1.8 profile default, which Browsertools, Udon and Browserdriver preserve through v10; focused real replay and synthesis checks pass. The failed report remains diagnostic. F08. |
| M86.6 | `[+]` | At clean OpenUdon `0f763e5ac45a85ae0890f011d4aef7e719deeb18` and exact clean sibling pins, current loopback passed 23/23 and journey passed 11/11. Both v2 reports independently verified with zero failed/skipped/quarantined cases; SHA-256 digests are recorded below. F01/F02/F08. |
| M86.7 | `[+]` | Passing commits, digests and counts are recorded above. Full Go tests, vet, `make check`, UWS validation, doc-memory, strict MkDocs, actionlint, and diff checks pass. Bounded review closed F08 and a stale help/guide description; no open P1/P2. |
| M86.8 | `[ ]` | Restore the desktop’s original `kernel.apparmor_restrict_unprivileged_userns=1` after all browser runs, then verify the setting. This requires the operator’s sudo authentication; this session cannot perform it. |

**Dependencies and order.** Completed M84 → completed M85 → M86.1 → M86.2 → M86.3 → M86.4 → M86.5 → M86.6 → M86.7 → M86.8. E04/E05 provide historical compatibility lineage, not reopened work. Current Make and hosted release checks consume M86.3; current-stack evidence depends on all preceding implementation and clean committed revisions. Each row is one commit unit under AGENTS.md.

**Acceptance.** The integration report passes all 19 named gates, including three opt-ins, with no skip/failure. The current scenario reports pass 23/23 loopback and 11/11 journey cases with no skip/failure/quarantine; inventory alone and `not_run` never qualify. Chromium launches with sandbox required, Firefox/WebKit launch in the installed-engine fixture, and temporary host settings are restored. The reports retain only closed value-free evidence and exact source identities. Full OpenUdon tests/vet, `make check`, local doc-memory, strict MkDocs, workflow validation, diff check, and bounded review pass. Failed attempts remain diagnostic and cannot be adopted as passing evidence.

**M86.4 evidence.** Clean sibling commits: Browsertools `9333a9f25dbb17551998a429e123e7a9ba976648`, Browserdriver `8c13b70d30a500e65e90a95a203493301b8b21a5`, UWS `e9b6181be0abb7f683fdb624d4dba282a59991d1`, Udon `080b8282e2b8f7ca7a9994b6d9f0e3d2891d853f` in separate `../udon-m86`. OpenUdon's Browsertools and UWS module requirements match the current lock. The host's AppArmor user-namespace restriction was temporarily set to `0` by the operator so Chromium could use its required sandbox. Playwright 1.62.1 Firefox/WebKit were installed locally. WebKit's missing Ubuntu codec libraries were unpacked under `/tmp/openudon-m86-libavif`, copied into the user's `~/.cache/openudon/browser-libs`, and linked into the installed browser's local `sys/lib` cache; no source checkout or system package was modified. WebKit launched again without a temporary preload after that relocation. Direct sandboxed Chromium, Firefox and WebKit launches succeeded before the matrix. The first matrix attempt (16 passed, 3 failed) is retained only as `eval/runs/browser-integration-m86/report-first-failed.json`; it exposed the Udon checkout's local sibling-replacement layout and WebKit's restricted browser environment. Both were corrected before the passing run. The passing report is `eval/runs/browser-integration-m86/report.json` with the digest in M86.4. These ignored local reports are not source artifacts.

**M86.5 diagnostic.** The initial current loopback report passed 23/23 and verified at clean `a63ca0758ba0b0d66f4ddc67f86b9cde41c9951c`, but it predates the M86.5 fix and cannot qualify the final source. The initial journey report at that revision passed 10/11 and failed `template-browser18` with `unclassified`; it remains under ignored `eval/runs/browser-scenario-m86-journey/` for diagnosis. Private local replay showed the HCL input path rejected the maximum signed integer with a range error; the exact conversion point was not established. The corrected case uses an exact numeric default in the reviewed Browser 1.8 JSON profile, while the string tag remains a workflow input. No published sibling source changed.

**M86.6 evidence.** `eval/runs/browser-scenario-m86-loopback/report.json`: `openudon.browser-scenario-eval.v2`, 23 passed, 0 failed, 0 skipped, 0 quarantined, SHA-256 `b3e80bba78c1b7c9ea612784b66d8a0c391f182a0bc5efb16a0cb22480c8ca40`. `eval/runs/browser-scenario-m86-journey/report.json`: `openudon.browser-journey-eval.v2`, 11 passed, 0 failed, 0 skipped, 0 quarantined, SHA-256 `ab56fb0ae10118be02bb7758d098c2dbfe3c0df0ebec24493d9490917ddfb6ce`. Both reports and sidecars were independently verified, bind clean OpenUdon `0f763e5ac45a85ae0890f011d4aef7e719deeb18`, and name the same clean Browsertools, UWS, Udon and Browserdriver commits listed above. The 11 journey results include the eight existing cases and all three new Browser 1.8/1.9/v10 cases. No public or real-account target was contacted.

**M86.7 review and checks.** Review iteration 1 inspected the current lock, manifest/report version split, modern profile/session boundaries, Make and hosted release routing, and the first real reports. It found F08, corrected in M86.5. Iteration 2 inspected the final diff and report provenance/safety claims, confirmed historical lock/manifests and public canaries were untouched, and corrected stale journey CLI/help wording. There are no open P1/P2 findings. At clean `54f049e15d54d3ef8d0b715b4cee6d67c00c383d`, `make check` (full `go test ./...`, standalone iCoT build, sibling and apitools boundary checks), `go vet ./...`, `go run ./cmd/openudon validate ./examples/uws-validation`, doc-memory from `tabilet/`, strict MkDocs, actionlint on `release.yml`, and both working-tree and baseline-to-HEAD `git diff --check` passed. After the wording correction, the affected CLI test and another full `go test ./...` passed. Passing report checks additionally found no sampled target or credential values; all participating source trees were clean. The operator must restore the original host AppArmor value for M86.8 to close.
