# v36 Result — Private registration discovery and portable recipes

The shared application owns a private registration inventory exposed explicitly
through iCoT and supervised application control. Operators can add missing routes
and account types, retain reduced native observation provenance, revise coverage
limitations, select a candidate into editable wizard fields, and review the exact
inventory revision. Review remains independent of unknown/partial coverage.

Owner-only storage outside Git and examples retains a bounded, hash-linked
revision history with exclusive writers and synced atomic files. Stale changes,
unsafe permissions, malformed or oversized history, unsupported claims and
interrupted writes fail closed. Ordinary snapshots, reviewed drafts and package
artifacts exclude inventory. Selection preserves consumed attempts, containment
and browser authority; it never starts a browser or changes existing recipe bytes.

UWS guidance now places discovery state in authoring tools. Regression coverage
verifies registration 1.1 recipes without discovery and preserves published
optional metadata, including the historical owner-reviewed value. Published
schemas, defaults, embedded archive and OpenUdon dependency pins are unchanged.
Typed private-input execution adoption, automatic crawling, complete live
qualification and W8M operation remain separate.

Full Go tests and vet pass in UWS and OpenUdon. OpenUdon make check and focused
inventory/UI race tests pass. All 18 sandbox-required loopback UI tests pass,
including inventory selection without target launch and restart persistence.
Bounded review and final documentation checks are recorded in
[A28](../memory-bank/status-A28.md).
