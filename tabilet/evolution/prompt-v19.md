# Typed MFA And Dashboard Output Authoring

Upgrade OpenUdon's one explicit live-browser boundary to strict Browsertools
author-session/result v2. The planner remains limited to focus, reviewed click,
bounded same-origin GET navigation, or human fallback. Only the human may
select one Browsertools-compatible MFA kind or declare final accessibility
outputs.

At completion, accept repeated
`output CANDIDATE_ID KEY TYPE LOCATOR_MODE` commands followed by `done`, show a
value-free sorted summary, and require final confirmation. Independently check
challenge kinds, symbolic credential slots, output identities and match proofs,
contexts, reviews, profile versions and contents, and secret absence before
atomic staging. Browser 1.7 must synthesize UWS 1.9; older profile pairs retain
their existing UWS 1.7/1.8 behavior.
