# Reviewed verification direction v38

Review detected verification dependencies in iCoT, bind authority v2 and transaction v4, preserve valid failed executor reports, and qualify the full synthetic 1.2 package handoff.

The approved release covers Turnstile, reCAPTCHA v2 and hCaptcha, including
invisible Submit activation. UWS owns portable contracts; provider APIs stay in
trusted adapters. Keep protection enabled, human challenges manual, private
values outside artifacts and consumed attempts immutable. Provider-network and
real account operations require separate scope. A30/E15 owns the bounded gate.
