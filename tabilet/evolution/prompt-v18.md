# Exact authenticated-authoring seam qualification

Close every critical, high, and medium integration-review finding across the
authenticated Browsertools/iCoT path. Accept the producer's actual review
discriminators, require exact bounds and portable context authority, reject
unsafe labels before disclosure, and constrain planner context references.

Replace component-local compatibility inference with named tests that consume
a real Browsertools envelope and replay its exact authentication/capability pair
through Udon and Browserdriver. Pin the coordinated UWS and Browsertools feature
commits, remove feature-test skips, preserve browser-free defaults, and make the
release matrix enforce these seams.
