# Prompt V17 - Authenticated Goal-Directed Browser Authoring

Add one explicit `icot browser-author live` path that coordinates a
Browsertools-owned headed Chromium context across human credential/MFA entry
and post-login goal exploration. Keep normal iCoT and agent mode non-executing,
send only reduced semantics through a strict local protocol, require explicit
provider/model disclosure plus action/origin/completion/staging gates, and keep
the deterministic Browsertools result under a restrictive private root.

Extend public UWS with additive popup/frame context contracts while preserving
all old profiles. Stage only independently validated canonical profiles and
safe review metadata. Replay them later through separate Udon runtime approval
and Browserdriver v3, never by transferring cookies, browser handles, or the
authoring context. Finish with one browser-free provider-free qualification
matrix across OpenUdon, Browsertools, UWS, Udon, and Browserdriver; keep real
installed-browser/site proof explicit and operator-authorized.
