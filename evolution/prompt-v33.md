# Evolution Prompt v33

Adopt published UWS 1.9.1 content trust as an additive, review-first OpenUdon
contract without changing ordinary validation, package approval, trusted-runner
authorization, or execution.

Let operators—not the LLM—declare reviewed provenance for package source
descriptions, operation outputs/defaults, triggers, and external workflow-entry
inputs. Emit UWS 1.9.1 only when those declarations are present; preserve UWS
1.9.0 and the existing package shape otherwise. Run the explicit core analyzer
with published Browsertools channel/output semantics during package review,
surface deterministic value-free findings as non-failing warnings, and qualify
the full advisory data-flow matrix against published Udon M37.

Do not write result v33 until A22, P06, and E12 all pass. Do not treat narrowing
as trust, expose runtime content, change executor/report wires, publish a
repository, create a tag or release, contact a live target, or start W8M W02
under this prompt.
