# Prompt V14 - Explicit Browser Authoring Handoff

Let iCoT coordinate a missing UI-capability profile through Browsertools without
silently becoming a live-browser agent. Emit exact external argv and review
gates, accept only reviewed value-free return artifacts, and keep private raw
evidence, credentials, MFA, cookies, storage, drivers, and sessions outside
OpenUdon.

Fail closed for login-required protected-page learning: the current headed
authentication observer destroys its context and cannot lend that session to
capability capture. Require separately reviewed authentication and capability
profiles until a new upstream context contract is designed.
